'use strict';

// const

class CacheProxy {
  constructor(client, config) {
    this.client = client;
    this.expire = config.expire;
  }

  // del(keys) {
  //   this.emi
  // }
}

module.exports = CacheProxy;
